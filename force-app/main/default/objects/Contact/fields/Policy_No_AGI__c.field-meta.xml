<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Policy_No_AGI__c</fullName>
    <description>SUBSTITUTE(Policy_No__c, &quot;_&quot;, &quot;&quot;)</description>
    <externalId>false</externalId>
    <formula>IF (LEFT(RIGHT(Policy_No__c,9),1) == &apos;-&apos;, LEFT(Policy_No__c,LEN(Policy_No__c)-9),
IF (LEFT(RIGHT(Policy_No__c,8),1) == &apos;-&apos;, LEFT(Policy_No__c,LEN(Policy_No__c)-8),
IF (LEFT(RIGHT(Policy_No__c,7),1) == &apos;-&apos;, LEFT(Policy_No__c,LEN(Policy_No__c)-7),
IF (LEFT(RIGHT(Policy_No__c,3),1) == &apos;-&apos;, LEFT(Policy_No__c,LEN(Policy_No__c)-3),
IF (LEFT(RIGHT(Policy_No__c,2),1) == &apos;-&apos;, LEFT(Policy_No__c,LEN(Policy_No__c)-2),
Policy_No__c)))))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Policy No AGI</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
