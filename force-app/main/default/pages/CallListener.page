<apex:page >
    <apex:includeScript value="/support/console/33.0/interaction.js"/>
    <apex:includeScript value="/support/console/33.0/integration.js"/>
    <script type="text/javascript">

        var doOnCallBegin = function (result) {
            //alert('The enclosing primary tab is about to be closed. Tab ID: ' + result.id + ', Object ID: ' + (result.objectId ? result.objectId : 'not available'));
            
        };

        var doOnCallEnd = function (result) {
            //alert('The enclosing primary tab is about to be closed. Tab ID: ' + result.id + ', Object ID: ' + (result.objectId ? result.objectId : 'not available'));
        };


        //Add a listener to handle the closing of the enclosing primary tab 
        if(sforce.console.isInConsole()){
            sforce.console.onCallBegin(doOnCallBegin);
            sforce.console.onCallEnd(doOnCallEnd);
        }
    </script>
</apex:page>